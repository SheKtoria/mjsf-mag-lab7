{
  "name": "shepel",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "docker-run": "docker build --tag shepel:latest . && docker run --rm -p 8080:80 --name shepel-instance shepel:latest",
    "docker-stop": "docker stop shepel-instance"
  },
  "dependencies": {
    "vue": "^3.3.4"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^4.2.3",
    "vite": "^4.4.5"
  }
}
